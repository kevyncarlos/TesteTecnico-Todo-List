# Teste Técnico - To-do list

### App with API and Web
* API: API .NET 8 for get and persist data.
* Web: Angularjs with listing and forms

### How execute?
1. First run the API, at that moment the database will be created and it will be ready to receive data; (F5 or 'dotnet run')
2. Run the web project, opening the index.html in any web browser;
3. Login as "kevyn" and password as "123"
3. (Optional) Open 'https://localhost:7089/swagger/index.html' for see and test endpoints.